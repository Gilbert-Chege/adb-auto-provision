# adb-auto-provision
ADB Auto Provision is a Python-based automation tool that continuously detects Android devices connected via ADB and runs predefined provisioning commands in parallel. It supports up to six devices, checks for full boot completion, improves reliability for factory and test environments.
